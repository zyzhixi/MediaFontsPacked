# 贡献指南

感谢你有兴趣参与 `.mfp` 格式规范的完善。本仓库是**协议层规范仓库**，不是某个客户端的实现仓库，
因此贡献的侧重点与一般代码项目不同。

## 本仓库管什么、不管什么

| 属于本仓库                             | 不属于本仓库                      |
| --------------------------------- | --------------------------- |
| `.mfp` 的字节布局、字段定义、约束语义            | 某个具体客户端的读写代码                |
| `manifest.json` 的字段与结构            | 增量更新算法、恢复记录生成算法的实现细节        |
| JSON Schema（可被各语言实现直接引用）          | 预览渲染引擎、字体解析库                |
| 最小与完整示例、错误码表、兼容性规则                | 平台相关的字体安装逻辑                 |

一句话判据：**「这个内容换一个客户端实现还成立吗？」** 成立则属于规范，不成立则属于实现
（实现相关内容请写到 [`IMPLEMENTATION.md`](./IMPLEMENTATION.md)，那是设计建议而非强制规范）。

## 修改规范的原则

1. **向后兼容优先**。必需字段（`mfpVersion`、`formatVersion`、`revision`、`fonts`、`checksum`）
   不可删除、不可重命名、不可改变语义。确需变更时**必须**提升 `formatVersion` 主版本号，
   并在 [`CHANGELOG.md`](./CHANGELOG.md) 中说明迁移路径。
2. **每条决策都要留理由**。规范中的设计说明统一采用「结论 + 理由」的写法（见 `SPEC.md` 第 10 章）。
   只给结论不给理由的条目会在评审中被要求补充——理由是将来修订时判断能否推翻该决策的依据。
3. **明确降级行为**。新增可选字段时，必须同时说明「该字段缺失时客户端应如何降级」，
   并更新 `SPEC.md` 第 6.3 节的降级表。
4. **同步更新四处**。改动字段后通常需要同步：
   `SPEC.md` 正文 → `schema/*.schema.json` → `examples/*.json` → `CHANGELOG.md`。
   漏改任何一处都会让示例与 Schema 失去参考价值。

## 提交流程

1. Fork 本仓库并新建分支，分支名建议 `spec/<主题>` 或 `fix/<主题>`
2. 修改后自检：
   - JSON Schema 能通过 `draft 2020-12` 校验器解析
   - `examples/minimal.json` 与 `examples/full.json` 能通过对应 Schema 校验
   - 文中交叉引用（章节号、文件名链接）未失效
3. 提交信息建议采用 Conventional Commits：`spec:`、`docs:`、`schema:`、`fix:`、`chore:`
4. 发起 Pull Request，在描述中说明：改了什么、为什么改、是否属于兼容性变更

## 提出议题

- **规范疑问**：说明你正在实现的场景与卡住的具体章节号
- **设计缺陷**：给出反例——在什么输入下现有规范会导致实现分歧或错误
- **新需求**：先说明它属于协议层还是客户端实现层（见上表），若属后者请移步具体客户端仓库

## 许可证

贡献即表示你同意你的内容以本仓库的 MIT 许可证发布。
